services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3003:8080"
    volumes:
      - election-data:/app/data
    environment:
      - DATABASE_URL=/app/data/election.db

volumes:
  election-data:
